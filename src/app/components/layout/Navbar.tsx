import React from "react";

const Navbar = () => {
  return (
    <nav className="w-full h-14 md:h-24 flex justify-between items-center border-b-2 text-white">
      <section className="italic text-xl uppercase font-semibold cursor-pointer">
        farhan
      </section>
    </nav>
  );
};

export default Navbar;
